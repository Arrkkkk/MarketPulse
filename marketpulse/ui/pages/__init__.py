"""Page scripts for st.navigation.

Each module here is executed as a Streamlit page, not imported as a normal
module — do not import from this package at load time, since every file
below runs Streamlit calls as a side effect of being read.
"""
